<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EVERNET SAS</title>
    <link rel="stylesheet" href="style.css">
</head>
<body> 
    <% if (cliente) { %>
        <div class="formulario">
             <h1>EDICIÓN DATOS DEL CLIENTE</h1>
             <form action="/actualizarCliente" method="post">
     
                 <input id="idCedula" name="idCedula" value="<%= cliente.cedulaCliente %>" hidden> <!--input oculto -->
                 
                 <div class= "fied">
                     <label>Nombres y Apellidos</label>
                     <input id="nombresCliente" type= "text" name="nombresCliente" value="<%= cliente.NombresApellidosCliente %>" autocomplete>
                 </div>
                 <div class= "fied">
                     <label>Cédula</label>
                     <input id="cedulaCliente" type= "number" name="cedulaCliente" value="<%= cliente.cedulaCliente %>" autocomplete>
                 </div>
                 <div>
                
                 <div class= "fied">
                     <label>Ciudad</label>
                     <input id="cityCliente" type= "text" name="cityCliente" value="<%= cliente.CiudadCliente %>" autocomplete>
                 </div>
                 <div class= "fied">
                     <label>Dirección</label>
                     <input id="dirCliente" type= "text" name="dirCliente" value="<%= cliente.DireccionCliente %>" autocomplete>
                 </div> 
                
                 <div class= "fied">
                     <label>Teléfono</label>
                     <input id="telefonoCliente" type= "number" name="telefonoCliente" value="<%= cliente.telefonoCliente %>"  autocomplete>
                 </div>
                 <div class= "fied">
                     <label>Email</label>
                     <input id="emailCliente" type= "email" name="emailCliente" value="<%= cliente.emailCliente %>" autocomplete>
                 </div>
                 <div class= "fied">
                     <label>Género</label>
                     <select id="generoCliente" name="generoCliente">
                         <option selected disabled>Seleccione una Opción</option>
                         <option <%= cliente.GeneroCliente=='Mujer' ? "selected": '' %> value="mujer">Mujer</option> <!--la línea de código de express indica que si el valor es igual a lo que se coloca, traiga el valor o sino deje el espacio vacío-->
                         <option <%= cliente.GeneroCliente=='Hombre' ? "selected": '' %> value="hombre">Hombre</option>
                         <option <%= cliente.GeneroCliente=='Otro' ? "selected":  '' %>value="otro">Otro</option>
                     </select>
                 </div>
                 <div>
                     <input type="submit" value="Agregar">
                 </div>
                 <div>
                     <a class="cancelarCliente" href="./registroClientes">Cancelar</a>
                 </div>
             </form>
       
         </div>
                 <% } %>
             

</body>
</html>